import {StyleSheet} from 'react-native';

export const appStyle = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'left',
    alignItems: 'right',
    backgroundColor: '#f5f7f5',
  },
  text: {
    fontSize: 32,
    fontWeight: 'italic',
    color: '#373',
  },
});